<Window x:Class="Nhom10_QuanLyLichThiDauBongDa.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Nhom10_QuanLyLichThiDauBongDa.ViewModels"
        Title="Đăng Nhập Hệ Thống" Height="450" Width="380"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundColor}">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,10,0,30" HorizontalAlignment="Center">
            <TextBlock Text="QUẢN LÝ GIẢI ĐẤU" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Center"/>
            <TextBlock Text="Đăng nhập để tiếp tục" FontSize="14" Foreground="#7F8C8D" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Tên đăng nhập:" FontWeight="SemiBold" Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource StandardTextBox}" Margin="0,0,0,20"/>

            <TextBlock Text="Mật khẩu:" FontWeight="SemiBold" Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Style="{StaticResource StandardPasswordBox}" Margin="0,0,0,10"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource AccentColor}" FontWeight="SemiBold" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,5,0,15" MinHeight="20"/>

            <Button Content="ĐĂNG NHẬP" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPassword}" Style="{StaticResource PrimaryButton}" Margin="0,0,0,10"/>
            <Button Content="THOÁT CHƯƠNG TRÌNH" Command="{Binding CloseCommand}" Background="Transparent" Foreground="#7F8C8D" BorderThickness="0" FontWeight="Bold" Cursor="Hand" Height="30"/>
        </StackPanel>
    </Grid>
</Window>